version: '3'
services:
  postgres:
    image: postgres
    container_name: postgres
    ports:
      - '6500:5432'
    restart: always
    env_file:
      - ./.env
    volumes:
      - postgres-db:/var/lib/postgresql/data
  # client:
  #   container_name: client
  #   build: ./
  #   ports:
  #     - '3000:3000'
  #   restart: always
  #   depends_on:
  #     - postgres
volumes:
  postgres-db:

